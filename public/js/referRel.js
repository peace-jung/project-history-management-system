var topnum = 0;
var bottomnum = 0;

/* $('.refer-center div button').click(function () {
  $('.refer-top > div').html(`<button id="top${topnum}" type="button" class="btn btn-lg"></button>`);
  $('.refer-bottom > div').html(`<button id="top${bottomnum}" type="button" class="btn btn-lg"></button>`);
  $('.refer-top > div').slideDown();
  $('.refer-bottom > div').slideDown();
});

$('.refer-top > div > button').on('click', function () {
  console.log('object');
  $(this).before(`<button id="top${++topnum}" type="button" class="btn btn-lg"></button>`);
});

$('button').on('click', function () {
  console.log();
}); */
